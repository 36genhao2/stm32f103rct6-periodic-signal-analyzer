# 数字滤波完整版实测说明

## 软件链路

```text
AD9220 4.000 MSPS
-> 31-tap Q15 FIR
-> 3倍抽取
-> 1.333333 MSPS、2048点FFT
-> 频率、Vpp、RMS、频谱分量、波形和串口屏
```

FFT前的原始采样数为6172点。波形显示使用4 MSPS全速滤后数据，不使用抽取后的低点密度数据。

## 2026-07-31串口实测

| 等效10倍测试信号 | 结果 |
|---|---|
| 200 kHz、1.500 Vpp正弦 | 199949 Hz，1466-1468 mVpp，517-518 mVrms，A1=732-733 mV |
| 1 MHz、2.000 Vpp干扰 | FIR后5-7 mVpp、1 mVrms，波形和频谱均NOSIGNAL |
| 500 kHz、1.000 Vpp + 1 MHz、2.000 Vpp | 499947-499948 Hz，493-494 mVpp，173 mVrms，仅保留500 kHz谱线 |
| 10.5 kHz，基波1.000 Vpp、三次0.600 Vpp、四次0.400 Vpp | 10479 Hz，A1=495 mV，A3=293 mV，A4=199 mV，429-431 mVrms |
| 250 kHz基波0.900 Vpp、500 kHz二次0.400 Vpp | 249971-249977 Hz，A1=448-452 mV，A2=191-198 mV，1115-1129 mVpp，344-345 mVrms |

实测波形处理约0.44秒，频谱处理约0.74秒，均小于2秒。

## 构建模式

- 当前工程的 `SERIAL_TEST_MODE=0`，用于TJC串口屏。
- `Artifacts/DigitalFull_UART_Test.hex` 是上述实测使用并已下载验证的串口测试固件。
- 最终接入真实放大器后，应把 `Measure.h` 中的 `GAIN` 改为实测增益，并完成幅频校准。

## 已知边界

本版本不使用模拟抗混叠滤波。1 MHz干扰实测有效，但当更高频干扰在4 MSPS采样前混叠到0~500 kHz通带内时，纯数字滤波无法恢复。
